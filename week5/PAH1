
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Layered Image Display</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap CSS (CDN) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .image-container {
      position: relative;
      width: 280px;
      height: 420px;
      margin: 2rem auto 0 auto;
    }
    .layered-img {
      position: absolute;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: box-shadow 0.2s;
      border-radius: 10px;
      box-shadow: 0 2px 14px rgba(40,40,40,.1);
    }
    /* Stacking visually distinct layers for initial state */
    #img1 { z-index: 1; }
    #img2 { z-index: 2; }
    #img3 { z-index: 3; }
    /* For a nice hint of stack, offset each slightly (optional) */
    #img2 { top: 12px; left: 12px; }
    #img3 { top: 24px; left: 24px; }
    /* Responsive for demo */
    @media (max-width:500px) {
      .image-container { width: 96vw; height: 60vw; max-width: 350px; max-height: 400px; }
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h2 class="mb-4 text-center">Layered Image Display</h2>
    <div class="d-flex justify-content-center mb-3">
      <button id="btn1" class="btn btn-primary mx-1">Image 1</button>
      <button id="btn2" class="btn btn-success mx-1">Image 2</button>
      <button id="btn3" class="btn btn-danger mx-1">Image 3</button>
    </div>
    <div class="image-container">
      <img id="img1" class="layered-img" src="image1.jpg" alt="img1">
      <img id="img2" class="layered-img" src="image2.jpg" alt="img2">
      <img id="img3" class="layered-img" src="image3.jpg" alt="img3">
    </div>
  </div>
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // Restore initial z-indexes: 1, 2, 3
      const images = [
        document.getElementById('img1'),
        document.getElementById('img2'),
        document.getElementById('img3'),
      ];
      images[0].style.zIndex = '1';
      images[1].style.zIndex = '2';
      images[2].style.zIndex = '3';

      // Utility to bring one image to front, rest behind
      function bringToFront(idx) {
        images.forEach((img, i) => img.style.zIndex = (i === idx ? '10' : '1'));
      }
      document.getElementById('btn1').addEventListener('click', () => bringToFront(0));
      document.getElementById('btn2').addEventListener('click', () => bringToFront(1));
      document.getElementById('btn3').addEventListener('click', () => bringToFront(2));
    });
  </script>
</body>
</html>
